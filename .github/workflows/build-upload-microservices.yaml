name: Build and push docker images
on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

jobs:
  build-push:
    runs-on: [ self-hosted ]
    permissions:
      contents: write
      pull-requests: write
      checks: write
    steps:
      - name: 'Checkout repository code'
        uses: actions/checkout@v4
      - name: 'Build and push docker images'
        run: |
          mvn clean install
          cd account-services
          docker build -t jayesh888/account-service:latest .
          docker push jayesh888/account-service:latest
          cd ../
          cd ledger-service
          docker build -t jayesh888/ledger-service:latest .
          docker push jayesh888/ledger-service:latest
          cd ../
          cd transaction-service
          docker build -t jayesh888/transaction-service:latest .
          docker push jayesh888/transaction-service:latest
